#!/sbin/busybox sh
export _PATH="$PATH"
export PATH=/sbin

if [ "$1" = 'secondary' ]; then
  busybox cp -a /vendor/samsung/init.rc.1 /init.rc
else
  busybox cp -a /vendor/samsung/init.rc.0 /init.rc
fi

mv /_init /init
exec /init
